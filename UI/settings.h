#ifndef SETTINGS_H
#define SETTINGS_H

#include <QMainWindow>

class Settings : public QMainWindow
{
    Q_OBJECT
public:
    explicit Settings(QWidget *parent = nullptr);

signals:

public slots:
};

#endif // SETTINGS_H